# Local FSAE RAG Pipeline dependencies
#
# Install PyTorch separately first with the CUDA wheel that matches the machine:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Then install the rest:
#   pip install -r requirements.txt

# PDF/document parsing and OCR
docling
pypdf

# Vector and metadata stores
lancedb

# Local LLM/VLM serving
ollama

# Embeddings and optional reranking
sentence-transformers
transformers

# Numeric/runtime support
numpy
pydantic
python-dotenv
tqdm

# API and browser UI
fastapi
uvicorn[standard]
streamlit
requests

# Legacy/experimental graph path retained in src/indexing.py
lightrag
networkx
scikit-learn

# Deep learning runtime, install CUDA wheels explicitly where possible
torch
torchvision

# Tests
pytest

