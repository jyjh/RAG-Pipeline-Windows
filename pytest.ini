[pytest]
addopts = -p no:cacheprovider --ignore=tests/browser
testpaths = tests
norecursedirs = .git .pytest_cache __pycache__ pytest-cache-files-*
markers =
    remote_client: run with the strict remote auth posture (no localhost auto-auth); unset, the suite exercises handlers as the trusted local operator
